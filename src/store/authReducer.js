export const addfirstNameAction=(firstName) => ({
    type : "ADD_FIRSTNAME",
    payload : firstName
})

lastName
email
password

const initialState = {:[]}

const authReducer = (state = initialState, action ) => {
    swith(action.type){
        case :
            return

        default :
        return state
    }
}

export default authReducer